<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Product Supplier App</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
  <style>
    body { background: #f0f2f5; }
    .navbar {
      background: linear-gradient(90deg, #0d6efd, #6610f2);
    }
    .card {
      border-radius: 16px;
      box-shadow: 0 6px 12px rgba(0,0,0,0.1);
    }
    .table thead {
      background: linear-gradient(90deg, #0d6efd, #6610f2);
      color: white;
    }
    .btn {
      border-radius: 8px;
    }
  </style>
</head>
<body class="container py-4">
  <nav class="navbar navbar-expand-lg mb-4 rounded-3">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold text-white" href="/">🏪 Product-Supplier</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item"><a class="nav-link text-white" href="/suppliers"><i class="bi bi-truck"></i> Suppliers</a></li>
          <li class="nav-item"><a class="nav-link text-white" href="/products"><i class="bi bi-box-seam"></i> Products</a></li>
          <% if (user) { %>
            <li class="nav-item">
              <a class="nav-link text-warning" href="/auth/logout">
                <i class="bi bi-box-arrow-right"></i> Logout (<%= user.username %>)
              </a>
            </li>
          <% } else { %>
            <li class="nav-item"><a class="nav-link text-white" href="/auth/login"><i class="bi bi-person"></i> Login</a></li>
            <li class="nav-item"><a class="nav-link text-white" href="/auth/register"><i class="bi bi-person-plus"></i> Register</a></li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>
